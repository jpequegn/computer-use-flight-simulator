# Generated Review Packet

This directory contains the JSON and Markdown packet generated by running the clean invoice fixture
through the real browser harness. Regenerate it from the repository root with:

```bash
npm run build
node dist/src/cli/main.js run invoice-clean --artifacts examples
```

The filenames are content IDs, so an unchanged logical run produces the same report names.
